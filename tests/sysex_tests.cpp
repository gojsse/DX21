#include <iostream>
#include "../src/sysex/VCEDCodec.h"

// M2 gate: round-trip tests for VCED/ACED/VMEM byte-for-byte fidelity
// Fixtures live in tests/fixtures/*.syx (not in version control)

int main() {
  std::cout << "OP4 Sysex Tests (M2 milestone)\n";
  std::cout << "TODO: load fixtures/, round-trip, assert byte-identical\n";
  return 0;
}
